{
  "extends": "../../tsconfig.base.json",
  "//": "references 三条都必须留：../contracts 与 ../config 被 src/audit 与 src/budget 直接使用（原因码注册表、预算上限与汇率默认值），../observability 提供审计脱敏复用的两张字段名表。../contracts 同时是 prisma/seed.ts 的前置——tsc -b 先产出它的 .d.ts，tsconfig.seed.json 的类型检查才能解析 @rag/contracts。",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    "tsBuildInfoFile": "dist/.tsbuildinfo"
  },
  "include": ["src/**/*.ts"],
  "references": [
    {
      "path": "../config"
    },
    {
      "path": "../contracts"
    },
    {
      "path": "../observability"
    }
  ]
}
