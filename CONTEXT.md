# 客服知识助手领域

> 项目状态总入口：请先阅读 [PROJECT_STATE.md](PROJECT_STATE.md)。本文件维护领域术语和概念边界；架构决策、评审状态和下一步任务以 `PROJECT_STATE.md` 及其链接的 ADR/设计文档为准。

本项目面向客服场景，基于可授权的产品资料、工单经验和标准话术形成可核验的处理建议。首期由客服人员使用，支持复制回复草稿，但不自动向终端客户发送。

## 业务概念

**客服知识助手**：面向客服业务的知识检索与证据回答系统，输出必须能回到授权来源。
_Avoid_: 通用企业知识库

**产品知识**：描述产品功能、配置、限制、版本差异和故障处理方式的正式资料。
_Avoid_: 产品宣传文案（除非被明确纳入可回答资料）

**工单知识**：从历史客服工单、故障记录和处理结果中沉淀的可复用问题模式与解决经验。
_Avoid_: 未脱敏的原始客户对话

**原始工单**：客服系统中包含具体客户、会话、订单和处理过程的原始业务记录，只能在受控场景中访问。
_Avoid_: 工单知识、可直接公开检索的知识文档

**候选工单知识**：由脱敏工单聚类和总结产生、尚未经过知识责任人审核的知识草稿。
_Avoid_: 已发布工单知识、原始工单

**标准话术**：经过业务审核、适用于特定问题和客户沟通场景的推荐表达。
_Avoid_: 模型自由生成的回复

**证据回答**：每个事实性结论都绑定到用户有权访问的产品知识、工单知识或其他正式来源的回答。
_Avoid_: 只展示来源列表的回答、无来源的流畅回答

**共享基座**：所有角色共同使用的身份、权限、文档、检索、引用、模型路由、观测和评测能力集合。
_Avoid_: 为每个角色复制一套系统

**角色工作台**：在共享基座上绑定特定知识空间、数据源、检索策略、回答策略和工具权限的使用视图。
_Avoid_: Git 分支、独立产品分叉

**知识空间**：具有独立成员、数据源、生命周期和检索策略的一组可治理知识资产。
_Avoid_: 仅按前端页面划分的目录

**共享知识资产**：具有唯一稳定身份和不可变版本、可被多个工作台按权限绑定的文档或结构化知识。
_Avoid_: 每个工作台各自上传的重复副本

**工作台成员**：业务用户在某个角色工作台中的成员关系和工作台内角色；同一业务用户可以加入多个工作台。
_Avoid_: 用户的全局唯一角色、所属部门

**知识权威级别**：知识发生冲突时用于决定事实可信顺序的业务属性；首期顺序为正式产品知识、标准话术、工单知识。
_Avoid_: 仅按更新时间排序、由模型临时选择

**适用范围**：一条知识可以正确使用的产品、版本、地区和生效时间组合。
_Avoid_: 普通展示标签

**知识责任人**：负责审核知识、标准答案、证据和失效处理的客服主管或产品专家。
_Avoid_: 平台研发、模型本身

**数据等级**：决定文档、查询和证据是否允许离开企业网络，以及可以调用哪些模型执行区的安全属性。
_Avoid_: 仅用于页面展示的密级标签

**模型执行区**：具有明确网络和数据外发边界的一组模型服务，首期分为云模型执行区和本地模型执行区。
_Avoid_: 具体模型名称、供应商名称

**身份提供方**：负责用户认证、登录会话、密码策略、MFA 和身份令牌签发的系统；首期采用 Keycloak，业务权限不归身份提供方单独决定。
_Avoid_: 业务用户表、文档 ACL

**业务用户**：在本项目中拥有租户、组织、部门、知识空间成员关系和审计身份的用户记录，通过身份提供方的稳定 subject 关联登录身份。
_Avoid_: 只存在于 Keycloak 的登录账号

**客户上下文**：当前客服问题适用的最少业务信息，首期包括产品、版本、地区、问题摘要和必要的受控标识。
_Avoid_: 客户完整档案、默认长期记忆

**流程验证 MVP**：使用合成或严格脱敏资料验证上传、解析、检索、引用、拒答和客服回复草稿闭环的最小版本。
_Avoid_: 真实用户试点、生产版本

**知识种子集**：为流程验证和回归评测人工准备的产品资料、标准话术、合成工单和黄金问题集合。
_Avoid_: 已覆盖真实业务长尾的正式知识库

**虚拟 SaaS 产品**：知识种子集使用的虚构业务对象，包含产品功能、套餐、版本、地区、退款、账号和故障处理规则。
_Avoid_: 随机且互不关联的演示文档

**模型预算**：限制模型调用成本的业务护栏，包括月度费用、单次 Token、每日调用和离线评测上限；MVP 首月费用上限为 500 元。
_Avoid_: 只有账单告警而没有调用阻断

**MVP 硬门禁**：流程验证版本必须满足的安全和功能条件，包括越权证据泄漏为零、主链路可运行、引用可回跳、无据问题拒答以及黄金题可重复运行。
_Avoid_: 尚未建立真实基线的候选质量数字

**候选质量目标**：在统一测量口径和真实基线建立前用于观察趋势的指标目标，包括 Recall@5、引用覆盖率、忠实度和时延，不直接阻断 MVP 交付。
_Avoid_: 已审计生产 SLO、当前实测成绩

**审核状态**：文档对外可见性的业务状态，首期为 Draft、PendingReview、Published、Archived；它与解析、索引和图谱处理状态分开管理。
_Avoid_: 用一个 status 字段同时表示审核和后台任务进度

**正交状态模型**：审核、文件安全、入库任务、步骤、索引投影、索引发布、删除保留和问答运行分别维护状态，每个状态只表达一个维度的事实。
_Avoid_: 为了页面方便把审核、解析、索引、删除和失败原因压进一个大枚举

**可检索条件**：由已发布、原文件可用、未删除、有效期内、必需投影就绪、索引发布已激活和当前用户有权访问共同决定的派生条件。
_Avoid_: 由接口直接写入的 `searchable=true`、只检查审核状态

**索引发布**：一组文档级和 Chunk 级投影经过构建、质量校验后，以版本化 Alias 原子激活的发布单元；保留上一发布用于回滚。
_Avoid_: Worker 直接覆盖当前线上索引、单个文档写入成功就视为发布成功

**索引激活意图**：记录候选 Release 切换到线上 Alias 的跨库操作意图、上一 Alias、尝试结果和对账状态，用于崩溃恢复或回滚。
_Avoid_: 只在 HTTP 请求里先改 PG 再直接切 OpenSearch，失败后没有恢复依据

**删除目标**：一次删除请求在 PostgreSQL、OpenSearch、MinIO、Redis 等副本上的独立执行目标和结果。
_Avoid_: 只把 `document.deleted=true` 当成所有副本已经删除

**可恢复 SSE 事件**：按 `run_id + seq` 持久化的可展示问答事件，断线后可续读或读取最终快照；不包含模型原始思考链。
_Avoid_: 只依赖内存连接，断线后无法知道回答和引用状态

**临时引用**：quick_parse 会话内的 `TEMPORARY` 引用，可在对象仍存在且当前 ACL 通过时回跳原文；TTL 或主动删除后转为 `EXPIRED`/墓碑，不属于长期正式知识来源。
_Avoid_: 把临时资料当作正式 Release 或永久证据

**ACL 作用域键**：由租户、知识空间、数据等级和可见性等级组合成的稳定索引过滤键，检索前由 PostgreSQL 授权事实编译为过滤条件。
_Avoid_: 把主体列表或授权版本号写进索引、用它替代权威授权判定

**候选权威复核**：候选合并后对候选文档版本集合做的一次批量 PostgreSQL 授权校验，覆盖文档级拒绝例外、删除墓碑、Legal Hold 和有效期，是最终授权判定点。
_Avoid_: 只依赖索引过滤结果、等待重投影生效后才拦下已撤权内容

**显式文档授权**：把某个 `document_version_id` 直接授予作用域之外主体的有界正向授权。阶段 1 不实现，仅作为已识别扩展点预留；因其是加法授权，只能进检索预过滤（复核阶段看不到非候选文档），预过滤编译须保留可追加 `OR document_version_id IN (...)` 子句的形状。
_Avoid_: 在阶段 1 建授权表/撤销/上限逻辑、把逐文档授权当作复核层事后补丁、把它写入 OpenSearch 主体列表

**全序冲突消解**：跨知识空间证据按权威级别、适用范围匹配度、生效时间、版本创建时间和文档版本 ID 的确定性全序排序；前两项并列且结论不相容时判为冲突并同时展示来源。
_Avoid_: 由模型自行裁决冲突、结果依赖知识空间遍历顺序

**预算账本**：PostgreSQL 中记录模型调用预扣、lease、结算和回收的表，是费用硬门禁；Redis 只缓存剩余额度用于展示和快速拒绝。
_Avoid_: 只在调用后统计费用、把 Redis 计数当作预算事实源

**回答快照**：AnswerRun 完成时写入对象存储的不可变正文、逐句引用和证据摘录，是回答正文的唯一长期副本。
_Avoid_: 把正文长期存在 PostgreSQL 或 Redis 中、把 SSE 续读窗当成归档

**注入风险等级**：文档内容与候选证据在解析、进入生成上下文前和回答输出后三处检测得到的注入风险状态，取值为无、可疑和阻断。
_Avoid_: 把注入检测当成一次性上传校验、把可疑内容直接送入生成上下文

**用户级配额**：单个用户的并发 AnswerRun、并发 SSE、提问频次和上传频次上限，超限只允许排队或拒绝。
_Avoid_: 通过跳过引用验证或缩小候选集来“满足”超限请求

**模型调用上下文**：由服务端生成并随每次 Chat、Embedding、Reranker 或引用验证调用传递的租户、用途、数据等级、执行区、预算预扣号、Trace 和取消信号；前端不得覆盖这些字段。
_Avoid_: 业务模块直接调用供应商 SDK、从请求参数决定数据等级或绕过预算门禁

**企业级基础 MVP**：范围可以只覆盖客服工作台和有限数据源，但身份权限、不可变版本、异步任务、索引发布、引用、审计、评测和恢复采用正式产品边界。
_Avoid_: 只完成文档向量化、相似度检索和聊天页面的一次性 Demo

**索引投影**：同一文档版本面向不同检索通道生成的可替换索引视图，包括关键词、向量和可选图谱投影。
_Avoid_: 把 PostgreSQL 文档记录直接当成全部检索索引

**候选索引 Release**：在 Draft/PendingReview 阶段构建并校验、但尚未绑定线上检索 Alias 的一组不可变索引投影；只有审核通过和发布命令成功后才可激活。
_Avoid_: 审核通过后才第一次构建线上索引、直接覆盖当前 Alias

**解析产物**：由 Parser Adapter 生成的规范化 Markdown、结构化块、原文定位和质量信息集合，是后续分块、引用和重建索引的输入。
_Avoid_: 只保存一段丢失页码和结构的纯文本

**图谱证据通道**：在权限、版本和证据约束下参与召回的实体关系投影；它是多跳问题的可选增强，不是默认事实来源。
_Avoid_: 无来源、无审核的自动关系直接驱动回答
